- hosts: localhost
  become: yes
  tasks:
    # - name: install nginx
    #   ansible.builtin.yum:
    #     name: nginx
    #     state: installed 
    - name: stop nginx
      ansible.builtin.service:
        name: nginx
        state: stopped  

